{
  "name": "",
  "version": "0.0.1",
  "main": "",
  "scripts": {
    "start": "cousin dev -e local",
    "build": "cousin build -e prod",
    "lint": "eslint . --fix --ext .js,.ts,.jsx,.tsx,.json",
    "prettier": "prettier --write"
  },
  "devDependencies": {},
  "dependencies": {},
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged",
      "commit-msg": "commitlint -e $HUSKY_GIT_PARAMS"
    }
  },
  "lint-staged": {
    "*.{js,json}": [
      "prettier --write",
      "eslint",
      "git add"
    ]
  },
  "engines": {
    "node": ">10.0.0"
  }
}
